mutation getPresignedUrl(
    $userId: String!
    $fileName: String!
    $fileExtension: String!
) {
    getPresignedUrl(
        input: {
            userId: $userId
            fileName: $fileName
            fileExtension: $fileExtension
        }
    ) {
        url
    }
}
